import React from 'react';

export default function Header() {
    return (
        <div className="header-outer">
            <h1 className="app-title">
                {'Draw polygons with a ruler and compass'}
            </h1>
        </div>
    );
}

